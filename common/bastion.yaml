apiVersion: apps/v1
kind: Deployment
metadata:
  name: bastion
  labels:
    naviator.github.com/devtemplate: bastion
spec:
  selector:
    matchLabels:
      naviator.github.com/devtemplate: bastion
  template:
    metadata:
      labels:
        naviator.github.com/devtemplate: bastion
    spec:
      terminationGracePeriodSeconds: 1
      containers:
      - name: main
        image: ghcr.io/naviator/bastion
        env:
        - name: USER_SSH_CONTENT
          value: /usr/ssh
        ports:
        - name: ssh
          containerPort: 2222
        volumeMounts:
        - name: user-ssh
          mountPath: /usr/ssh
        resources:
          limits:
            memory: 1Gi
            cpu: 2000m
          requests:
            memory: 1Mi
            cpu: 1m
      volumes:
      - name: user-ssh
        secret:
          secretName: user-ssh
          optional: false
          defaultMode: 0644
---
apiVersion: v1
kind: Service
metadata:
  name: bastion
spec:
  selector:
    naviator.github.com/devtemplate: bastion
  ports:
  - port: 22
    targetPort: ssh
---
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: bastion-egress
spec:
  podSelector:
    matchLabels:
      naviator.github.com/devtemplate: bastion
  policyTypes:
  - Egress
  egress:
  - ports:
    - protocol: TCP
      port: 2222
    - protocol: UDP
      port: 53
    - protocol: TCP
      port: 53
